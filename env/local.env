# Local development profile (committed — no secrets)
# Use with: npm start   or   NEVERLOST_PROFILE=local npm start
NEVERLOST_PROFILE=local
SKETCHFAB_REDIRECT_URI=http://localhost:5173/library
SKETCHFAB_ALLOWED_ORIGINS=http://localhost:5173
