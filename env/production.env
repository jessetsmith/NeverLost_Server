# Production profile for testing deployed OAuth locally (committed — no secrets)
# Use with: npm run start:production   or   npm run dev:production
NEVERLOST_PROFILE=production
SKETCHFAB_REDIRECT_URI=https://jessetsmith.github.io/NeverLost/library
SKETCHFAB_ALLOWED_ORIGINS=https://jessetsmith.github.io
